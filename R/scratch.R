

bb_goscatter(simMatrix = module_summary_0.8$`Module 9`$simMatrix, 
             reducedTerms = module_summary_0.8$`Module 9`$reducedTerms,size = "score",
             addLabel = T,
             labelSize = 4)

# meeting on June 29:

# do pseudobulk comparison using all B cells between timepoints and show volcanoes for them.
